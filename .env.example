# The frontend proxies /api/* to the Python (FastAPI) backend via Next.js
# rewrites (see next.config.mjs), so requests are same-origin — no CORS/cookie
# issues. This is the backend origin the proxy forwards to.
API_PROXY_TARGET=http://localhost:8000
